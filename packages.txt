# Apt package selections.
# to use this (probably need sudo):
#  dpkg --set-selections < packages.txt
#  apt-get -u dselect-upgrade
git install
irssi install
python-jinja2
python-markdown   install
python-pip        install
python-requests   install
vim install
workrave install
