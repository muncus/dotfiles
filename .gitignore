# swap files
.*.sw?
# compiled java and python files
.*.class
*.pyc
# include these so they are excluded from git status in the dotfiles repo.
.vim/bundle/
.vim/.netrwhist
venv/
.vim/%*
# terraform 
.terraform/
terraform.tfstate
terraform.tfstate.backup
.terraform.lock.hcl
